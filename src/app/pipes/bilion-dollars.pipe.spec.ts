import { BilionDollarsPipe } from './bilion-dollars.pipe';

describe('BilionDollarsPipe', () => {
  it('create an instance', () => {
    const pipe = new BilionDollarsPipe();
    expect(pipe).toBeTruthy();
  });
});
